---
import { getFilterName } from '../../lib/filterableHelper';

interface Props {
    listName: string;
    tagName: string;
}

const { listName, tagName } = Astro.props;
---

<label for={getFilterName(listName, tagName)} title={`filter by tag: ${tagName}`}><slot /></label>
